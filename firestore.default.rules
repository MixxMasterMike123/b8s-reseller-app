rules_version = '2';

// The app uses ONLY the named database `b8s-reseller-db` (see firestore.rules).
// This file locks down the unused (default) database completely.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
